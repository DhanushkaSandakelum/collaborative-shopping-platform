server:
  port:
    7002

spring:
  application:
    name:
      sms

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true